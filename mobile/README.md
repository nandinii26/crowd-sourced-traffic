# Mobile (Android Kotlin) App

This folder contains a minimal Android app skeleton showing:
- Location consent flow
- Background location collection service (optimized)
- Sending anonymized events to backend `/ingest`

Open the `android-app` folder in Android Studio to run the app.
